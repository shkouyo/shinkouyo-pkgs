name: reconcile-on-push

on:
  push:
    branches:
      - main
    paths:
      - packages/**

permissions:
  contents: read

jobs:
  plan:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    outputs:
      build_matrix: ${{ steps.plan.outputs.build_matrix }}
      remove_matrix: ${{ steps.plan.outputs.remove_matrix }}
      has_builds: ${{ steps.plan.outputs.has_builds }}
      has_removals: ${{ steps.plan.outputs.has_removals }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          fetch-depth: 0

      - name: Compute package changes
        id: plan
        run: |
          set -eu
          before="${{ github.event.before }}"
          after="${{ github.sha }}"
          diff_file="$RUNNER_TEMP/diff.txt"
          git diff --name-status "$before" "$after" -- 'packages/*.sh' > "$diff_file"
          sh scripts/ci/package-diff-plan.sh "$diff_file" >> "$GITHUB_OUTPUT"

  remove:
    needs: plan
    if: needs.plan.outputs.has_removals == 'true'
    strategy:
      fail-fast: false
      max-parallel: 1
      matrix: ${{ fromJson(needs.plan.outputs.remove_matrix) }}
    uses: ./.github/workflows/_remove-package.yml
    with:
      package: ${{ matrix.package }}
    secrets: inherit

  build:
    needs:
      - plan
      - remove
    if: ${{ always() && needs.plan.outputs.has_builds == 'true' && (needs.plan.outputs.has_removals == 'false' || needs.remove.result == 'success') }}
    strategy:
      fail-fast: false
      matrix: ${{ fromJson(needs.plan.outputs.build_matrix) }}
    uses: ./.github/workflows/_build-package.yml
    with:
      package: ${{ matrix.package }}
    secrets: inherit

  publish:
    needs:
      - plan
      - remove
      - build
    if: ${{ always() && !cancelled() && needs.plan.outputs.has_builds == 'true' && (needs.plan.outputs.has_removals == 'false' || needs.remove.result == 'success') }}
    uses: ./.github/workflows/_publish-staged.yml
    secrets: inherit
